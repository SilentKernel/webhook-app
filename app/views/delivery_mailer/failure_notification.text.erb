Webhook delivery failed
=======================

Hi <%= @user.first_name %>,

<% if @delivery.can_retry? -%>
A delivery attempt to "<%= @destination.name %>" has failed.
Retries remaining: <%= @delivery.max_attempts - @delivery.attempt_count %>.
<% else -%>
A delivery to "<%= @destination.name %>" has permanently failed after <%= @delivery.attempt_count %> attempts.
<% end -%>

Delivery Details
----------------
Destination: <%= @destination.name %>
URL: <%= @destination.url %>
Event Type: <%= @event.event_type || "N/A" %>
Attempts: <%= @delivery.attempt_count %> / <%= @delivery.max_attempts %>

View the delivery details: <%= delivery_url(@delivery, locale: :en) %>

View all failed deliveries: <%= deliveries_url(status: 'failed', destination_id: @destination.id, locale: :en) %>

---

You're receiving this email because you subscribed to failure notifications for this destination.

To stop receiving these emails, manage your subscriptions in your profile settings:
<%= edit_user_registration_url(locale: :en) %>
